openapi: 3.0.3
info:
  title: Granet Technologies - Task Manager API
  description: >
    REST API for managing tasks (Create, Read, Update, Delete), backed by
    MySQL. Every response uses a consistent envelope:
    `{ "success": bool, "data"?: ..., "error"?: string }`.
  version: "1.0.0"
servers:
  - url: http://localhost:8080/api
    description: Local development

tags:
  - name: Tasks
  - name: Health

paths:
  /health:
    get:
      tags: [Health]
      summary: Health check
      description: Used by container orchestrators / uptime monitors.
      servers:
        - url: http://localhost:8080
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthEnvelope"

  /tasks:
    get:
      tags: [Tasks]
      summary: List tasks
      description: Supports optional search, status filter, and sort.
      parameters:
        - name: search
          in: query
          schema: { type: string }
          description: Case-insensitive match against title or description.
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/TaskStatus" }
        - name: sortBy
          in: query
          schema:
            type: string
            enum: [due_date_asc, due_date_desc, created_at_desc]
            default: created_at_desc
      responses:
        "200":
          description: List of tasks
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Task" }
        "400":
          $ref: "#/components/responses/BadRequest"

    post:
      tags: [Tasks]
      summary: Create a task
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TaskInput" }
      responses:
        "201":
          description: Task created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Task" }
        "400":
          $ref: "#/components/responses/BadRequest"

  /tasks/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: integer, format: uint64, minimum: 1 }

    get:
      tags: [Tasks]
      summary: Get a task by ID
      responses:
        "200":
          description: Task found
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Task" }
        "404":
          $ref: "#/components/responses/NotFound"

    put:
      tags: [Tasks]
      summary: Update a task (full replace)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TaskInput" }
      responses:
        "200":
          description: Task updated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Task" }
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      tags: [Tasks]
      summary: Delete a task
      responses:
        "200":
          description: Task deleted
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/Envelope"
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          id: { type: integer, format: uint64 }
        "404":
          $ref: "#/components/responses/NotFound"

components:
  schemas:
    TaskStatus:
      type: string
      enum: [pending, in_progress, completed]

    Task:
      type: object
      properties:
        id: { type: integer, format: uint64, example: 1 }
        title: { type: string, example: "Design database schema" }
        description: { type: string, example: "Model the tasks table with proper indexes" }
        dueDate: { type: string, format: date, example: "2026-07-20" }
        status: { $ref: "#/components/schemas/TaskStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
      required: [id, title, dueDate, status, createdAt, updatedAt]

    TaskInput:
      type: object
      properties:
        title:
          type: string
          maxLength: 150
          example: "Write onboarding docs"
        description:
          type: string
          maxLength: 2000
          example: "Cover setup, architecture, and FAQ"
        dueDate:
          type: string
          format: date
          example: "2026-08-01"
        status:
          $ref: "#/components/schemas/TaskStatus"
      required: [title, dueDate, status]

    Envelope:
      type: object
      properties:
        success: { type: boolean }
      required: [success]

    HealthEnvelope:
      type: object
      properties:
        success: { type: boolean, example: true }
        data:
          type: object
          properties:
            status: { type: string, example: ok }

    ErrorEnvelope:
      type: object
      properties:
        success: { type: boolean, example: false }
        error: { type: string, example: "title is required" }
      required: [success, error]

  responses:
    BadRequest:
      description: Validation error
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    NotFound:
      description: Task not found
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
